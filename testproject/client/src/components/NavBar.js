import React from 'react';
// we don't have prop types yet but we will import it

const NavBar = () => (
    <nav style={{backgroundColor: 'grey', height: '50px', marginTop: '-12px', color: 'white'}}>
        <p style={{marginLeft: '15px'}}>The Met</p>
    </nav>
);

export default NavBar;