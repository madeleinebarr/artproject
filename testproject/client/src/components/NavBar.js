import React from 'react';
// we don't have prop types yet but we will import it

const NavBar = () => (
    <nav className="navBar">
        <img className="navBarMetLogo" src="../metlogo.png" alt="met logo"></img>

    </nav>
);

export default NavBar;